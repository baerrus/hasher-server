# This file defines the external dependencies and settings for the Bazel build system.

module(
    name = "hasher-server",
    version = "0.2.1",
)

bazel_dep(name = "platforms", version = "0.0.11")
bazel_dep(name = "asio", version = "1.34.2")